# Точные версии чистых Python-зависимостей torch 2.5.0 nv24.08.
# Разрешены pip на плате 2026-09-14 и проверены там же вместе с torch
# на GPU. Без закрепления каждая пересборка образа получала бы новые
# версии с PyPI, и «тот же образ» перестал бы значить то же самое.
#
# numpy — отдельный разговор, см. install.sh.
#
# ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО — jinja2 и markupsafe. torch требует jinja2
# любой версии, и apt-овый 3.0.3 это удовлетворяет. Закреплённая версия
# заставила бы pip поставить свою в /usr/local поверх apt, а оттуда её
# импортирует и cloud-init, для которого jinja2 — зависимость пакета.
# Правило для всего списка: закрепляется только то, что pip ставит
# в любом случае (замер установкой в venv поверх системы, 2026-09-14).
numpy==1.26.4
# scipy поднимается вместе с numpy: apt-овый 1.8.0 (его тянут matplotlib
# и pandas) с numpy 1.26 при каждом импорте предупреждает, что версия
# не поддерживается. 1.13.1 — последняя с поддержкой numpy 1.x и Python 3.10.
scipy==1.13.1
filelock==3.32.6
fsspec==2026.7.0
mpmath==1.3.0
networkx==3.4.2
sympy==1.13.1
typing_extensions==4.16.0
# Сборочные зависимости torchvision (--no-build-isolation берёт их
# из системы, а не тянет свежие во временное окружение).
pillow==11.3.0
