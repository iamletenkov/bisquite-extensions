# Разрешено pip на плате 2026-09-14 вместе с колесом NVIDIA и проверено
# там же обучением модели на GPU. Отступления от «самой свежей» версии —
# ниже, с причинами.
#
# keras: pip берёт свежайший 3.x (на дату замера 3.12.4), а TF 2.16
# выпускался и тестировался с keras 3.3. На 3.12 простая свёртка
# работает, но гарантий совместимости нет — закрепляем ту, с которой
# вышел TF. Обучение на GPU с 3.3.3 проверено.
keras==3.3.3
# numpy и scipy — те же, что у l4t-pytorch, и по тем же причинам.
numpy==1.26.4
scipy==1.13.1
#
# ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО — requests, urllib3, idna, certifi, six,
# packaging. Их требования apt-овые версии удовлетворяют, и pip их не
# трогает; закрепление заставило бы поставить свои в /usr/local поверх
# apt, а оттуда их импортирует cloud-init (requests — его зависимость).
# Закреплено только то, что pip ставит в любом случае: замер установкой
# в venv поверх системы, 2026-09-14. Из apt при этом перекрываются семь
# пакетов — gast, markupsafe, mpmath, numpy, protobuf, scipy, sympy;
# cloud-init с markupsafe 3.0.3 шаблоны рендерит (проверено там же).
absl-py==2.5.0
filelock==3.32.6
fsspec==2026.7.0
mpmath==1.3.0
networkx==3.4.2
sympy==1.13.1
astunparse==1.6.3
flatbuffers==25.12.19
gast==0.7.0
google-pasta==0.2.0
grpcio==1.83.1
h5py==3.16.0
libclang==18.1.1
markdown==3.10.3
markdown-it-py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
ml-dtypes==0.3.2
namex==0.1.0
opt-einsum==3.4.0
optree==0.20.0
protobuf==4.25.9
pygments==2.21.0
rich==15.0.0
tensorboard==2.16.2
tensorboard-data-server==0.7.2
tensorflow-io-gcs-filesystem==0.37.1
termcolor==3.3.0
typing-extensions==4.16.0
werkzeug==3.1.8
wrapt==2.4.1
