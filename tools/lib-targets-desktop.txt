# Куда вендорится lib/bisquite-desktop — CLI ручек рабочего стола
# (автологин, автоблокировка, затемнение). Формат — как у lib-targets.txt.
#
# Только те, кто ставит менеджер входа и сессию: ручки применяет служба
# ДО менеджера входа, и ставить её должен тот, кто ставит менеджер.
extensions/debian/gnome
extensions/debian/xfce4
extensions/debian/lxde
# Не ставит менеджер, но на первой загрузке зовёт `apply --now` —
# правки ручек, пришедшие после старта менеджера.
extensions/debian/cloud-user-desktop
