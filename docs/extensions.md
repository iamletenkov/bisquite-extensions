# Конвенция расширений

Документ описывает, как устроено расширение bisquite, что значит каждое поле
манифеста `extension.yaml`, как добавить новое расширение, где расширение
лежит в госте и как сборка доставляет общий код из `lib/`.

Спека — `docs/specs/2026-09-02-extensions-system.md` основного репозитория.
Оба её этапа закрыты: конвенция и проверки живут здесь, в подмодуле,
а читатель манифестов — инструкция `EXTENSION` и команды `bs extension` —
в `bisquite`. Кто из них что читает, разобрано ниже: не все девять полей
попадают в резолвер, и делать вид, что попадают, было бы враньём в другую
сторону.

## Содержание

- [Что такое расширение](#что-такое-расширение)
- [Как расширение подключается](#как-расширение-подключается)
- [Две фазы: сборка и первый запуск](#две-фазы-сборка-и-первый-запуск)
- [Где живут параметры времени выполнения](#где-живут-параметры-времени-выполнения)
- [Библиотека настроек `bisquite-conf`](#библиотека-настроек-bisquite-conf)
- [Манифест `extension.yaml`](#манифест-extensionyaml)
- [Словарь способностей](#словарь-способностей)
- [Раскладка в госте и общий код `lib/`](#раскладка-в-госте-и-общий-код-lib)
- [Проверки](#проверки)
- [Как добавить расширение](#как-добавить-расширение)
- [Что осталось незакрытым](#что-осталось-незакрытым)

## Что такое расширение

Расширение — каталог `extensions/<группа>/<имя>/` с `install.sh` внутри.
Наличие `install.sh` и есть признак расширения: именно его запускает сборка
после того, как каталог целиком уехал в гостя.

```vmfile
EXTENSION x11vnc
```

Отсюда следует всё остальное в этом документе. **До гостя доезжают два
каталога: каталог расширения** — в `/opt/bisquite/<имя>/` — **и `lib/` корня
источника**, который расширение видит как `/opt/bisquite/<имя>/lib`. Не
репозиторий, не соседние расширения, не `extensions/`. Всё, кроме общего кода
из `lib/`, каталог расширения обязан нести сам. Подробности —
[«Раскладка в госте и общий код `lib/`»](#раскладка-в-госте-и-общий-код-lib).

Раскладка каталогов резолвером **не используется**: расширение ищется по полю
`name` манифеста, а bisquite обходит кеш источника целиком (`rglob` по
`extension.yaml`). Группировка по семейству — удобство автора источника;
другой источник вправе разложить дерево иначе, и привязка к раскладке сделала
бы правым того, кто угадал чужую. Побочное следствие: опечатка в имени
каталога ничего не ломает, а опечатка в `name` даёт громкий отказ — правильная
сторона для громкости.

Типовой состав:

```
<имя>/
├── extension.yaml              # манифест (описан ниже)
├── install.sh                  # фаза build: ставит софт, включает сервис донастройки
├── configure.sh                # фаза firstboot: донастройка под конкретную машину
├── configure-<имя>.service     # systemd-oneshot, гоняет configure.sh на загрузке
├── knobs                       # опционально: схема настроек домена (/etc/bisquite/<домен>/config)
├── knobs.apply, knobs.secret   # опционально: хуки применения и секретов
└── README.md
```

Копий `get_cloud_user.sh` и `bisquite-desktop` в каталоге нет: в госте рядом
со скриптами лежит ссылка `lib`, и зовут их как `"$SCRIPT_DIR/lib/…"`.

Запускается сборкой **только `install.sh`**. `configure.sh` bisquite не зовёт
никогда: его ставит и включает сам `install.sh`, кладя `configure-<имя>.service`
в `/etc/systemd/system/`. Отсюда и путь в юните — `/opt/bisquite/<имя>/configure.sh`,
абсолютный и переживающий сборку. Каталог расширения поэтому **остаётся**
в образе: он и есть рабочий каталог служб, а `extension.yaml` рядом говорит,
какая версия стоит.

Путь в госте — публичный контракт: на него ссылаются юниты расширений и
команды первой загрузки в манифестах записи. Сменить его значит поднять
`layout` и мажорные версии всех расширений с юнитами, как это сделано при
переезде из `/opt/vmsetup` (раскладка 1) в `/opt/bisquite` (раскладка 2).

## Как расширение подключается

### `EXTENSION` — основная форма

```vmfile
EXTENSION gnome
EXTENSION core/docker
EXTENSION x11vnc X11VNC_PORT=5901 X11VNC_LISTEN=all X11VNC_PASSWORD="two words"
```

Форма: `EXTENSION [источник/]имя [КЛЮЧ=ЗНАЧЕНИЕ ...]`. Инструкция
разворачивается в один вызов `virt-customize`, и всё до запуска скрипта —
операции appliance (`--mkdir`, `--copy-in`, `--chmod`, `--link`):

1. `lib/` источника — в `/opt/bisquite/lib/<sha256>/`, скриптам `+x`;
2. каталог расширения — в `/opt/bisquite/<имя>/`, `--chmod 0755` каждому
   `*.sh` верхнего уровня;
3. ссылка `/opt/bisquite/<имя>/lib → /opt/bisquite/lib/<sha256>`;
4. `--run-command` с `/opt/bisquite/<имя>/install.sh`.

Расширение без `layout: 2` bisquite отвергает ещё в резолвере, до скачивания
базы и appliance.

**Параметры уезжают в `install.sh` переменными окружения как есть**, без
таблицы соответствия. Соблазн был обратный — писать `port=5901` и переводить
в `X11VNC_PORT` где-то в резолвере, — и отвергнут по измеренной причине:
таблица устарела бы молча, ровно как разъехались список поддерживаемых сюит
в расширении `docker` и содержимое репозитория Docker. Единственный источник
правды о том, какие переменные читает расширение, — его `install.sh`. Поэтому
и README каждого расширения называет их его именами.

Имя параметра проверяется грамматикой переменной окружения POSIX ещё при
разборе VMFILE: `2FOO=bar` — отказ здесь, а не неразбираемая строка в госте.

Два свойства, и оба несущие:

- **права ставятся `--chmod`, а не `RUN_COMMAND chmod`.** Вторая форма
  выполняет код в госте и на чужой архитектуре отказывает; `--chmod` идёт
  инструментом appliance — тот же довод, по которому рядом стоит `--mkdir`.
  Маски у `--chmod` нет, поэтому бит ставится каждому скрипту поимённо;
- **применимость вычисляется, а не предполагается** — см. раздел про фазы.

### Откуда берётся каталог

Из локального кеша источников, и **только из уже синхронизированного**: ни
сборка, ни валидация в сеть за расширением не ходят. Кеш живёт в
`<DATA_DIR>/extensions/<имя источника>/`, наполняет его отдельный явный шаг
`bs extension sync` по объявлению в `<DATA_DIR>/data/extensions.yaml`:

```yaml
sources:
  - name: core
    type: git
    url: https://github.com/iamletenkov/bisquite-extensions.git
    ref: v1.1.0
  - name: lab
    type: path
    path: /srv/lab-extensions
```

Источник `core` — этот репозиторий — объявлен уже в поставляемом с пакетом
файле, поэтому обычно достаточно `bs extension sync`. Свой `extensions.yaml`
в каталоге данных **замещает** поставляемый целиком, а не дополняет его.
Типа `builtin` (везти дерево внутрь пакета) нет намеренно: за одну сессию
расширение `docker` получило четыре правки, и внутри пакета каждая ждала бы
релиза bisquite — расширения меняются быстрее инструмента.

Источник в ссылке необязателен: `EXTENSION docker` ищет по всем источникам
в порядке объявления и берёт первый, как `PATH`. Затенение поставляемого
`core` своим источником — рабочий приём, и о нём пишется строка в журнал
(`extensions.shadowed`). А вот два расширения с одним `name` **внутри одного**
источника — громкий отказ: это ошибка самого источника.

Ниже `<DATA_DIR>/extensions/<источник>/` раскладка не предписана — там лежит
дословная копия дерева источника. Для `core` это значит, что манифест
оказывается на пути `<DATA_DIR>/extensions/core/extensions/debian/docker/`.

Смотреть и проверять — `bs extension sources`, `bs extension ls`,
`bs extension check <ссылка>`. Последняя печатает отчёт и при отказе, называя
причину полем `refusal`, а код возврата при этом 1 — по нему её и ставят в CI.

### Ручной формы `COPY_IN` больше нет

Прежняя запись — `COPY_IN <чекаут>/extensions/debian/<имя>:/opt/vmsetup/`
плюс `RUN_COMMAND …/install.sh` — с раскладкой 2 не работает, и это
осознанно: ссылку `lib` рядом с каталогом расширения ставит только
`EXTENSION`, а юниты смотрят в `/opt/bisquite`. Повторять доставку руками
значило бы держать в VMFILE отпечаток `lib` и ссылку на него — ровно то, что
в цепочке `FROM` разойдётся первым.

### `extensions/openwrt/` расширениями не является

`extensions/openwrt/uci-defaults/` и `extensions/openwrt/wrt_cloudinit/`
живут в том же дереве, но по этой конвенции не расширения, и манифестов у них
нет намеренно:

- **Другой механизм доставки.** Не каталог в `/opt/bisquite/<имя>` плюс
  `install.sh`, а `UPLOAD` конкретных файлов по конкретным путям
  (`/etc/uci-defaults/80-rootfs-resize`, `/etc/init.d/wrt.cloudinit`).
- **Ни одна фаза их не описывает честно.** В госте при сборке не выполняется
  ничего — только копирование файлов, а `COPY_IN`/`UPLOAD` libguestfs умеет
  и при несовпадении архитектур. Объявить `phase: build` значило бы
  потребовать совпадения архитектур и отвергнуть `amd64/openwrt/openwrt.vmfile`,
  который сегодня собирается на amd64-хосте и работает. Объявить
  `phase: firstboot` тоже нельзя: спека прямо исключает эту фазу для OpenWrt,
  потому что libguestfs не умеет ставить туда firstboot-службу.

Валидатор их пропускает и **печатает список пропущенных**, а не молчит.
Появится третья фаза (или явная пометка «только файлы») — их можно будет
завести; до тех пор запись в манифесте была бы враньём.

Со стороны bisquite это же решение продублировано запретом:
`OpenWrtBuilder` блокирует директиву `EXTENSION` целиком, и отказ приходит
до сборки, а не изнутри гостя. Оба довода те же — расширения рассчитаны на
apt и systemd, а libguestfs не умеет ставить в OpenWrt firstboot-службу.

## Две фазы: сборка и первый запуск

| | `build` | `firstboot` |
|---|---|---|
| Кто запускает | сборка, внутри `virt-customize` | systemd-oneshot на устройстве |
| Когда | при сборке образа | при первой загрузке |
| Что можно | ставить пакеты, править файлы, `systemctl enable` | всё, что зависит от конкретной машины |
| Сеть | сеть машины сборки | сеть устройства в поле |

**Границу между фазами проводит не удобство, а природа задачи.** В `build`
уезжает всё, что должно быть одинаковым на всём тираже: `INSTALL` запекает
пакет в образ, и все устройства получают идентичное содержимое. В `firstboot`
остаётся только то, что **по природе** делается на устройстве: привязка к
пользователю, которого создаёт cloud-init (при сборке его имени нет),
генерация ключей и сертификатов, донастройка под конкретное железо.

**`firstboot` не должен ходить в сеть, и сегодня не ходит ни один.** Строка
«сеть устройства в поле» в таблице выше описывает, что там *доступно*, а не
что там *позволено*: устройство может уехать туда, где сети нет вовсе, и
скачивание на первой загрузке молча не доедет — образ загрузится, а куска
в нём не окажется. Проверять это на стенде бесполезно, потому что на стенде
сеть есть.

Замер 2026-09-12 по всему дереву: 26 расширений, из них 12 с фазой
`firstboot`, и сетевой вызов был ровно один — `snap install chromium`
в расширении `chromium`. Обойти его внутри расширения нечем: нативного
Chromium для arm64 нет ни в Ubuntu ports, ни в репозиториях NVIDIA —
`chromium-browser` там транзитный пакет к снапу, `firefox` на jammy устроен
так же, — то есть зависимость была не в реализации, а в самом пакете.
Расширение удалено. Браузер без снапа даёт расширение `firefox`: Mozilla
собирает amd64 и arm64 сама и публикует `.deb` в `packages.mozilla.org`,
установка идёт в `build` (проверено на AGX Orin 2026-09-14). Из репозиториев
дистрибутива остаются `epiphany-browser` (WebKitGTK) и `falkon` (QtWebEngine).

Практическое следствие для нового расширения: если работа требует
скачивания, ей место в `build`, а `firstboot` пусть доводит уже лежащее
в образе. Если скачивание в `firstboot` неизбежно — это признак, что
пакет выбран неверно.

**Сам cloud-init в сеть на первой загрузке тоже не идёт.** Бисквит пишет
в `user-data` `package_update: false` и `package_upgrade: false`
(`infrastructure/device/cloud_init.py`), а расширение `nocloud-cidata`
прибивает `datasource_list` к `[NoCloud, None]`, то есть перебора сетевых
датасорсов не будет. Сид читается с раздела `cidata` на самом носителе.

Перенести установку пакета из `build` в `firstboot`, чтобы обойти
несовпадение архитектур, — не оптимизация, а подмена гарантии надеждой:
каждое устройство пойдёт в репозиторий в свой момент времени и может
получить другую версию.

Расширение вправе объявить обе фазы — так устроено большинство: `install.sh`
ставит софт и включает `configure-<имя>.service`, а тот на первой загрузке
гоняет `configure.sh`.

Ограничение libguestfs, из которого растёт вся возня с архитектурой:
совместимыми считаются только `x == y` и `x86_64 → i386`, остальное — **отказ,
а не деградация**. Граница проходит по слою: `FROM`, `COPY_IN`, `UPLOAD` и
завершающий `virt-sysprep` на amd64-хосте с arm64-образом работают, а
`UPDATE`/`INSTALL`/`RUN_COMMAND` — нет. `EXTENSION` выполняет код внутри
гостя, поэтому попадает под тот же барьер.

### Что из этого вычисляет bisquite

Фазу объявляет автор, а **применимость считается** из пары «архитектура
хоста ↔ архитектура образа» (`LABEL arch`):

| Объявлено | Архитектуры | Что происходит |
|---|---|---|
| `phase: build` | совпадают | выполняется при сборке |
| `phase: build` | различаются | **отказ** с указанием, где собирать |
| `phase: firstboot` | любые | выполняется |

Отказ называет причину полностью — «расширение X объявляет фазу build, то есть
выполняет код внутри гостя, а хост amd64 и образ arm64 — разные архитектуры»,
— и, если у расширения фаза `firstboot` тоже объявлена, отдельно говорит,
почему ею нельзя подменить `build`. Это и есть запрет из абзаца выше,
записанный в коде, а не пожелание.

Первая строка таблицы проверяется раньше остальных: если `LABEL arch` образа
вообще нет в поле `arch` манифеста, отказ приходит до разговора про фазы.
А `host_arch is None` (платформа не из известных) пропускается: сверять не
с чем, и барьер `LABEL arch` устроен так же — заводить здесь второе, более
строгое поведение значило бы объяснять, почему они разные.

**Расширение, объявившее только `firstboot`, сегодня даёт отказ.** Не потому,
что так задумано, а потому, что такого расширения в дереве нет ни одного
(замер 2026-09-03: девять манифестов, у всех есть `build`), и ветка под него
была бы непроверенным кодом на пути сборки. Отказ говорит это прямо.

## Где живут параметры времени выполнения

Раздел отвечает на вопрос оператора: **устройство уже работает, как
поменять настройку и чем её применить.**

Ответ один на все расширения с настройками: **`bisquite-conf`**.

```bash
bisquite-conf list                                   # домены со схемой
bisquite-conf show x11vnc                            # ключи, значения, умолчания, откуда
sudo bisquite-conf set x11vnc X11VNC_LISTEN=all      # проверка, запись, применение
echo 'пароль' | sudo bisquite-conf set x11vnc X11VNC_PASSWORD=-   # секрет со stdin
```

Адрес — **домен**, каталог в `/etc/bisquite/`, а не имя расширения: `desktop`
ставят четыре расширения, `vino` принадлежит `vino-vnc`, `sensing-camera` —
`sensing-gmsl2-camera`.

| Домен | Файл | Схему ставит | Кто читает | Чем применяет `set` (хук `.apply`) |
|---|---|---|---|---|
| `desktop` | `/etc/bisquite/desktop/config` | `gnome`, `xfce4`, `lxde`, `cloud-user-desktop` (`lib/knobs/desktop`) | `bisquite-desktop.service` на каждой загрузке | `bisquite-desktop apply`: сразу, если менеджер входа работает |
| `x11vnc` | `/etc/bisquite/x11vnc/config` | `x11vnc` | `EnvironmentFile` юнита `x11vnc@.service` | перезапуск включённого `x11vnc@<пользователь>` |
| `selkies` | `/etc/bisquite/selkies/config` (0600) | `selkies` | `EnvironmentFile` юнита `selkies@.service` | объявление для Teleport + перезапуск `selkies@*` |
| `vino` | `/etc/bisquite/vino/config` | `vino-vnc` | `configure.sh` → dconf пользователя | перезапуск `configure-vino-vnc` (сервер берёт порт и адрес при старте сессии) |
| `sensing-camera` | `/etc/bisquite/sensing-camera/config` | `sensing-gmsl2-camera` | `bisquite-sensing-camera-ctl` в юнитах камер | перезапуск `bisquite-sensing-camera@*`, `bisquite-sensing-clock` |
| `teleport` | `/etc/bisquite/teleport/config` (0600) | `teleport-agent` | `bisquite-teleport render` на старте агента | `render` + reload агента; кластер — только `bisquite-teleport join` |
| `code-server` | `/etc/bisquite/code-server/config` (0600) | `code-server` | `configure.sh` → `~/.config/code-server/config.yaml` | перезапуск `configure-code-server` |
| `kiosk` | `/etc/bisquite/kiosk/config` | `kiosk` | `configure.sh`, `run-kiosk.sh` | перезапуск `configure-kiosk` |
| `chromium-kiosk` | `/etc/bisquite/chromium-kiosk/config` | `chromium-kiosk` | `configure.sh` → `/etc/chromium-kiosk/config.yml` | перезапуск `configure-chromium-kiosk` |

Остальные расширения действующих настроек не имеют: они однократно
настраивают систему, и повторить это можно только повторным прогоном
`configure-<имя>.service`. `/etc/bisquite/teleport/apps.d/` — не настройки
домена, а объявления других расширений; библиотека его не трогает.

Формат у всех один — `KEY=VALUE` с грамматикой systemd `EnvironmentFile`.
YAML-файлы `code-server`, `kiosk` и `chromium-kiosk` (формат bisquite, а не
приложения: `configure.sh` генерирует из него настоящий конфиг) переносятся
при установке расширения поверх старого образа и удаляются.

### Открытие службы в сеть — отдельное решение, а не смена адреса

Умолчания прослушивания осознанно локальные (`localhost` у `x11vnc` и
`vino`, `127.0.0.1` у `selkies`). Меняя их, проверьте, чем закрыт доступ: у
`code-server` связка `CODE_SERVER_BIND=0.0.0.0` с пустым паролем отдаёт в сеть
шелл под пользователем, который обычно состоит в группе `sudo`, — то есть
машину целиком.

Вторая альтернатива — веб-приложение Teleport (`teleport-agent`): служба
остаётся на петле, а расширение кладёт объявление в
`/etc/bisquite/teleport/apps.d/<имя>.conf` — ровно `NAME` и `URI` на петле.
Кому приложение видно, решает метка `env` оператора, а не объявление;
формат и проверки — README `teleport-agent`.

Альтернатива без открытия порта и без кластера — туннель, он не требует правки
конфигурации вовсе:

```bash
ssh -L 9001:127.0.0.1:9001 <пользователь>@<машина>
```

## Библиотека настроек `bisquite-conf`

`lib/bisquite-conf` — одним файлом библиотека (`source "$SCRIPT_DIR/lib/bisquite-conf"`)
и CLI (`/usr/local/sbin/bisquite-conf` — ссылка, как у `bisquite-desktop`; CLI
находит себя через `readlink -f`, то есть всегда в том `lib/<sha256>`, которым
его поставили). Спека — `docs/specs/2026-09-15-extension-knobs-library.md`
основного репозитория.

До неё у каждого расширения были свои **правила**: два исполняли файл
настроек как shell (`source`, `.`), манифесты правили файлы `sed -i`,
который молчит, если ключа нет, `install.sh` переписывал файл целиком и
стирал правки оператора при повторной установке, а применить правку можно
было тремя разными способами.

### Схема

Файл `knobs` в каталоге расширения (общая схема — `lib/knobs/<домен>`) —
единственное объявление настроек домена:

```
# ключ            тип                      умолчание   описание
X11VNC_PORT       port                     5900        порт RFB
X11VNC_LISTEN     enum:localhost,all       localhost   адрес прослушивания
X11VNC_PASSWORD   secret:hook              —           пароль (в файл не пишется)
X11VNC_PASSFILE   ro:path                  —           пишет только хук
TELEPORT_ENV      "re:^[a-z0-9 ]+$"        —           поле в кавычках может содержать пробелы
SELKIES_*         str                      —           шаблон: любой ключ с префиксом
```

| Тип | Значение |
|---|---|
| `bool` | `0` или `1` |
| `int`, `port` | целое; порт 1–65535 |
| `host`, `path` | имя хоста или IP; абсолютный путь |
| `str` | одна строка без управляющих символов, `$` и обратных кавычек |
| `enum:a,b` | одно из перечисленных |
| `re:ERE` | полное совпадение с регулярным выражением bash (якоря пишет автор) |
| `list:<тип>` | через запятую, каждый элемент — своим типом |
| `secret`, `secret:<тип>` | маскируется в `show`; ограничен только переводом строки (и типом); файл получает `0600` |
| `secret:hook` | в файл не пишется: значение уходит хуку `<домен>.secret` по stdin |
| `ro:<тип>` | `bisquite-conf set` отказывает; пишет владелец (`bisquite-teleport join`, хук) |
| `<тип>?` | пустое значение допустимо и при непустом умолчании (флаги, которые можно очистить) |

`—` — пустое умолчание. **Пустое значение принимает только ключ с пустым
умолчанием или типом с `?`**: `X11VNC_PORT=` сломал бы службу. Шаблон `PREFIX_*` открывает
пространство имён, словарь которого — свойство версии приложения (Selkies);
явно объявленные ключи с тем же префиксом проверяются своим типом. Схема
разбирается библиотекой, а не `yq`: манифест `extension.yaml` читает bisquite
на хосте, а схема нужна скриптам в госте.

### Регистрация: последний установивший побеждает

`conf_init <домен> <схема>` в `install.sh` ставит ссылки
`/opt/bisquite/knobs/<домен>`, `<домен>.apply`, `<домен>.secret` (хук, которого
у нового владельца нет, снимается) и `/usr/local/sbin/bisquite-conf`. В цепочке
`FROM` дочерний слой несёт более новую схему — ровно как ссылка
`bisquite-desktop`.

### Чтение и запись

- **Разбор — не `source`**, а конечный автомат `parse_env_file_internal()`
  из systemd: кавычки снимаются, `\` экранирует, `\<перевод строки>` склеивает,
  `#`/`;` — комментарии, последнее присваивание побеждает. Файл годится и
  юниту, и скрипту одновременно; `$(…)` в нём остаётся строкой. Сверено с
  systemd 257 (`tools/test-conf.sh`). Одно расхождение между версиями systemd:
  до v254 комментарий, оканчивающийся `\`, съедал следующую строку (на Jetson
  с Ubuntu 22.04 — v249); писатель таких комментариев не создаёт.
- **Запись атомарная** (временный файл в том же каталоге + `mv`), комментарии
  и порядок сохраняются, ключ заменяется на месте первого вхождения, дубликаты
  убираются; значение экранируется (`\`, `"`) так, что systemd прочтёт его
  байт в байт. `0600`, если в схеме есть секрет; закрытый оператором файл не
  ослабляется.
- **`conf_init` не перезаписывает заданное**: создаёт файл, дописывает
  недостающие ключи с непустым умолчанием, а параметры VMFILE (`--env`:
  экспортированные ключи схемы из окружения `install.sh`) ложатся поверх через
  проверку. Поэтому в `install.sh` нельзя присваивать умолчания
  экспортированным переменным (`X_PORT="${X_PORT:-5900}"`): для `--env` это
  выглядело бы параметром VMFILE.
- **`KEY=-`** читает значение со stdin: секрет не попадает в argv и
  `/proc/<pid>/cmdline`. Пустой stdin — отказ, а не пустое значение: у
  `secret:hook` пустое значение снимает пароль, и `echo "$UNSET" | …` не должен
  открывать VNC. Очистить ключ — явным `KEY=`.
- **Разовая миграция YAML** (`--migrate-yaml`): скаляры, один уровень
  вложенности (`SECTION.KEY`), списки в обеих формах (`[a, b]` и `- a`).
  Строка, которую не удалось разобрать, — отказ, YAML остаётся на месте;
  значения ключей вне карты называются в журнале, а не пропадают молча.
- **`secret:hook`**: значение уходит по stdin хуку `<домен>.secret KEY`, тот
  превращает его в то, что читает служба (x11vnc — файл `-rfbauth`, vino —
  base64), и печатает производные пары (`X11VNC_PASSFILE=/etc/x11vnc/passwd`),
  которые записываются через ту же проверку. Хук отказал — **не записано
  ничего** и код ненулевой: образ, который выглядит защищённым паролем, но
  поднимает `-nopw`, невозможен. Оговорка: файл пароля хук меняет сам, до
  записи конфига; откажи затем запись (диск, блокировка) — путь в конфиге
  прежний, а файл уже новый или удалён. Для x11vnc это fail-closed: путь
  без файла — отказ старта, а не `-nopw`.

### Применение

`bisquite-conf set` после записи зовёт хук `<домен>.apply` со списком
изменённых ключей, **кроме** трёх случаев:

- `systemctl is-system-running` отвечает `starting` или `initializing` —
  первая загрузка, `runcmd`, `guestfs-firstboot`: `set` только пишет и говорит
  «применится при старте служб». `firstboot-commands` исполняются внутри
  `cloud-final.service`, а службы `configure-code-server` и
  `configure-vino-vnc` стоят `After=cloud-final`: блокирующий перезапуск
  оттуда — взаимная блокировка (замер на Jetson Nano, шесть минут чёрного
  экрана);
- `--no-apply` — то же явно;
- systemd не запущен (сборка).

`--apply` зовёт хук и во время загрузки — так делают `bisquite-desktop set` и
`bisquite-teleport set`, чьи хуки безопасны. Обратная сторона правила: служба,
которая к моменту `set` уже запущена (x11vnc и Selkies поднимаются после
`graphical.target`, не после `cloud-final`), без `--apply` прочтёт правку только
на следующем старте — поэтому примеры манифестов зовут `set --apply` для
`x11vnc` и `selkies`. **Хук обязан быть неблокирующим**
(`systemctl --no-block restart …`), и валидатор отказывает хуку с
`systemctl start|restart` без `--no-block`. Хук отказал — запись уже
состоялась, код ненулевой.

### Манифесты вместо `sed -i`

```yaml
firstboot-commands:
  - "bisquite-conf set x11vnc X11VNC_LISTEN=all"
  - >-
    bisquite-conf set selkies
    SELKIES_ADDR=0.0.0.0
    BISQUITE_SELKIES_ALLOW_NO_AUTH=true
  - "bisquite-conf set code-server CODE_SERVER_BIND=0.0.0.0"
```

Опечатка в ключе или значении — ненулевой код и текст в
`cloud-init-output.log`, а не молчаливый пропуск.

### Функции для скриптов расширения

| Функция | Что |
|---|---|
| `conf_init <домен> <схема> [--env] [--migrate-yaml YAML=KEY,…] [KEY=VALUE…]` | `install.sh`: регистрация, файл, недостающие ключи, параметры поверх, разовая миграция YAML |
| `conf_load <домен>` | каждый ключ схемы (и шаблонный из файла) — переменная оболочки того же имени: значение файла или умолчание |
| `conf_get <домен> KEY` | значение или умолчание |
| `conf_set <домен> [--allow-ro] KEY=VALUE…` | проверка, хуки секретов, атомарная запись; без применения |
| `conf_validate <домен> [--allow-ro] KEY=VALUE…` | только проверка |
| `conf_check <домен>` | проверка того, что лежит в файле сейчас (правка руками) — перед тем, как значение уйдёт в YAML или аргументы |
| `conf_keys <домен>` | ключи схемы по порядку |
| `conf_cli_set [--apply\|--no-apply] <домен> KEY=VALUE…` | `bisquite-conf set` целиком, для обёрток вроде `bisquite-desktop set` |

Библиотека не зовёт `exit` и не полагается на `set -e` вызывающего: каждая
функция возвращает ненулевой код и печатает причину. Все пути учитывают
`BISQUITE_CONF_ROOT` — на нём работают тесты, без root.

## Ручки рабочего стола

Автологин, автоблокировка и затемнение — **не поведение расширения, а ручки**,
общие для `gnome`, `xfce4`, `lxde` и `cloud-user-desktop`. Все булевы, все
по умолчанию `0` (до 2.0.0 этих расширений всё это включалось безусловно).

| Ручка | `0` (умолчание) | `1` |
|---|---|---|
| `DESKTOP_AUTOLOGIN` | экран входа | вход без пароля |
| `DESKTOP_AUTOLOGIN_USER` | пусто — пользователь cloud-init | имя |
| `DESKTOP_DISABLE_SCREEN_LOCK` | поведение ОС | без автоблокировки; вручную заблокировать можно |
| `DESKTOP_DISABLE_SCREEN_BLANK` | поведение ОС | без затемнения, гашения, DPMS и сна |

Схема ручек — `lib/knobs/desktop`, домен `desktop` библиотеки `bisquite-conf`;
`bisquite-desktop set` — то же, что `bisquite-conf set --apply desktop`.

**Где задаётся:**

| Уровень | Как | Когда применяется |
|---|---|---|
| сборка | `EXTENSION gnome DESKTOP_AUTOLOGIN=1` | умолчание образа в `/etc/bisquite/desktop/config` |
| образ профиля | `FIRSTBOOT_COMMAND "bisquite-desktop set …"` | guestfs-firstboot, до менеджера входа — на первой же загрузке |
| compose | строкой в `firstboot_commands` | так же, это тот же guestfs-firstboot |
| манифест записи | строкой в `firstboot-commands` | cloud-final, ПОСЛЕ старта менеджера входа: `set` применяет сразу и перезапускает менеджер, если нет открытой сессии |
| работающая машина | `sudo bisquite-desktop set …` (или `sudo bisquite-conf set desktop …`) | сразу / хуком, если система не загружается |

Все примеры с рабочим столом включают три ручки строкой
`bisquite-desktop set DESKTOP_AUTOLOGIN=1 DESKTOP_DISABLE_SCREEN_LOCK=1 DESKTOP_DISABLE_SCREEN_BLANK=1`.

**Кто применяет.** `bisquite-desktop.service` — на **каждой** загрузке, до
менеджера входа, `After=cloud-init.service guestfs-firstboot.service`. Рёбер
к `cloud-config`/`cloud-final` нет намеренно: первый ждёт `snapd.seeded`,
связка со вторым уже дважды давала молчаливый цикл, и systemd выбрасывал gdm.
До старта менеджера служба:

1. вносит пользователя в `video render audio pulse input dialout plugdev` —
   **до** сессии. Без `video` Xorg автологина не открывает GPU
   (первая загрузка AGX Orin 2026-09-14: 52 падения и экран входа);
2. пишет автологин в gdm3 (`daemon.conf`/`custom.conf`) и lightdm;
3. блокировка и затемнение — системная база dconf **с locks** (пользователь
   в настройках не перебьёт), экран входа gdm, `Xsession.d` для DPMS, xfconf,
   lxsession, `xserver-command` lightdm. Выключенная ручка удаляет свои файлы.

**Что сделано на железе:** `gnome` на AGX Orin — ручки в обе стороны,
перезагрузки, циклов нет. `xfce4`, `lxde` и Jetson Nano с новым механизмом
не проверялись.

Общий код — `lib/bisquite-desktop`; юнит — в теле скрипта, подкоманда
`install`. Расширение зовёт `"$SCRIPT_DIR/lib/bisquite-desktop" install
"$SCRIPT_DIR"`, и `install` ставит CLI **ссылкой**
`/usr/local/sbin/bisquite-desktop → /opt/bisquite/<имя>/lib/bisquite-desktop`,
а не копией. Копии в `/usr/local/sbin` не на кого опереться: `get_cloud_user.sh`
лежит рядом с оригиналом, и скрипт находит его через свой настоящий путь
(`readlink -f`). Ссылка же всегда приводит CLI и резолвер пользователя из
одного `lib/`. Каталог расширения из образа не удаляется — из него работают
и юниты, — поэтому ссылка не повиснет. Прежний `/usr/local/lib/bisquite-desktop`
(копия `get_cloud_user.sh` до раскладки 2) `install` удаляет.

## Манифест `extension.yaml`

```yaml
name: docker
version: 3.0.0
layout: 2
family: deb
arch: [amd64, arm64]
phase: [build, firstboot]
provides: [container-runtime]
requires: []
conflicts: []
```

Набор полей закрыт: девять полей, ни больше ни меньше. Неизвестное поле роняет
и валидатор здесь, и разбор манифеста в bisquite (`extra="forbid"`), и это
осознанно — поле, которого не знает читатель манифеста, молча ничего не
делает, а выглядит как объявление.

**Читателей два, и читают они разное.** Различие несущее: половина полей
влияет на сборку, половина существует только для проверки.

| Поле | bisquite | `tools/validate-extensions.py` |
|---|---|---|
| `name` | ищет по нему расширение | сверяет с именем каталога |
| `version` | кладёт в родословную образа | требует semver `X.Y.Z` |
| `layout` | отказывает, если не `2` | требует ровно целое `2` |
| `family` | сверяет с ответом `virt-inspector` | требует один из четырёх |
| `arch` | сверяет с `LABEL arch` | требует непустой список |
| `phase` | вычисляет применимость | требует значения из набора |
| `provides` | только печатает | требует, чтобы `requires` кем-то закрывались |
| `requires` | только печатает | ищет циклы в графе |
| `conflicts` | только печатает | требует симметрии |

Три последние строки — не придирка. **Топологической сортировки и отказа по
конфликту в bisquite нет**, поэтому порядок слоёв в VMFILE по-прежнему
держится вниманием автора, а не объявленным графом. Считать иначе — самая
дорогая ошибка при чтении этого документа.

### `name`

Имя расширения. Им на расширение ссылается `EXTENSION`, и резолвер сверяет
именно это поле, а не имя каталога. Валидатор дополнительно требует их
совпадения — не ради резолвера, а ради человека: `conflicts` у соседа
именует расширение, а искать его будут по каталогу.

Алфавит тот же, что у сегмента ссылки: `[A-Za-z0-9_][A-Za-z0-9._-]{0,63}`.

### `layout`

Раскладка в госте, которую ждёт расширение. Сегодня единственное значение —
`2`: каталог в `/opt/bisquite/<имя>/`, общий код — через
`/opt/bisquite/<имя>/lib`. Сверка идёт на хосте, до appliance, в обе стороны:

- новый bisquite отказывает расширению без `layout: 2` с текстом «расширение
  старой раскладки — синхронизируйте источник». Старые юниты смотрят
  в `/opt/vmsetup`, и зелёная сборка дала бы мёртвые службы;
- старый bisquite отказывает на новом поле сам: манифест у него разбирается
  с `extra="forbid"`.

Проверка внутри гостя (`test -f lib/...` в `install.sh`) не заменяет поле:
в цепочке `FROM` её обошёл бы `lib`, оставленный базой. Смена раскладки —
мажорные версии расширений, чьи пути в госте меняются.

### `version`

Semver `X.Y.Z` — так требует валидатор. Версия самого расширения (конвенции
его файлов), не версия софта, который оно ставит. Версия всего набора —
git-тег репозитория.

Bisquite поле **не проверяет** и решателя версий не имеет: значение
информативное, оно уезжает в родословную собранного образа. Отсутствующее
поле он читает как `0.0.0`, поэтому строгость здесь держится валидатором,
а не сборкой.

### `family`

Семейство пакетного менеджера: `deb`, `rpm`, `apk`, `openwrt`.

**Это не «дистрибутив» под другим именем, и разница принципиальна: у семейства
есть проверяющий, а у дистрибутива нет.** `virt-inspector` отдаёт рядом
с архитектурой прямой ответ:

```xml
<package_format>deb</package_format>
<package_management>apt</package_management>
```

Дистрибутив осью не является **нарочно**. Гость знает о себе точнее любой
объявленной таблицы: `/etc/os-release` не устаревает молча, а таблица
устаревает. Поэтому «на каком дистрибутиве мы оказались» — ветвление внутри
`install.sh` (как в `docker`, где ветвление идёт по признакам железа),
а не поле манифеста. Замер подтверждает: `virt-inspector` на 64-битной
Raspberry Pi OS отвечает `<distro>debian</distro>`, потому что она честно
объявляет `ID=debian`; ось «targets: raspios» вошла бы в противоречие
с фактом.

Проверяющий не гипотетический: перед поиском расширения bisquite один раз
на сборку зовёт `virt-inspector` по самому образу, достаёт из ответа
`<package_format>` и **отбирает кандидатов по нему**. Не разобрался
инспектор — семейство считается неизвестным и из отбора выпадает; это
смягчение, а не отказ.

Отсюда два следствия, которых нет у валидатора. Bisquite **не держит списка
допустимых семейств**: словарь `virt-inspector` шире четырёх (`pkgng`,
`ebuild`, `pacman`), и второй список в коде мог бы только разойтись с первым.
Опечатка в поле ловится тем, что расширение просто не находится, — отказ
называет и семейство, по которому шёл отбор. Список из четырёх значений
(`deb`, `rpm`, `apk`, `openwrt`) — правило **этого репозитория**, и держит
его `tools/validate-extensions.py`.

**`family` — поле манифеста, а не имя каталога.** Каталог называется
`extensions/debian/`, а семейство внутри — `deb`. Расхождение оставлено
намеренно: каталог — удобство автора, а называть семейство по каталогу значило бы завести второе имя для того же
понятия.

### `arch`

Непустой список архитектур гостя, на которых расширение применимо: `amd64`,
`arm64`, …

Архитектура — единственная объявляемая ось применимости, потому что она
**ограничение пары хост↔гость на этапе сборки**: внутри гостя её не спросишь,
гостя ещё нет.

Написание — OCI, и оно канонизируется при разборе: `aarch64` в файле и `arm64`
в `LABEL arch` — одно и то же. Различать их было бы ложным расхождением на
каждом arm64-расширении, потому что словари ядра и OCI не совпадают. Сегодня
канонических значений два — `amd64` и `arm64`; всё остальное bisquite
отвергает, называя список.

Заполняется по факту, из кода:

- расширение ставит только apt-пакеты → обе архитектуры;
- расширение качает бинарь по ссылке, зависящей от архитектуры → те
  архитектуры, для которых релиз опубликован (так у `code-server`: ссылка
  на mkcert собирается из `dpkg --print-architecture`, и это сняло прежнее
  ограничение `amd64`);
- расширение прибивает метапакет (`linux-headers-amd64` в `nvidia`) или идёт
  в репозиторий, где нет второй архитектуры (там же, PPA `graphics-drivers`)
  → только `amd64`.

Объявить архитектуру — значит пообещать, что она работает. Поэтому `armhf`
нет ни у одного расширения, хотя релизы под него существуют: 32-битные цели
проектом не собираются и не проверялись.

### `phase`

`build`, `firstboot` или список из обоих. Что из объявленного выполнится —
вычисляется, см. [Что из этого вычисляет bisquite](#что-из-этого-вычисляет-bisquite).

Валидатор дополнительно предупреждает (не роняет) о рассогласовании
с файлами: объявлен `firstboot`, а `configure.sh` рядом нет — или наоборот.
Скаляр и список равноправны: `phase: build` принимается наравне
с `phase: [build]`, потому что так написана половина живых манифестов
и смысл записи однозначен.

### `provides`

Способности, которые расширение даёт. Способность — не имя пакета, а то, на
что может сослаться сосед. Три десктопа ставят `xorg xinput` и потому дают
**одну и ту же** способность `x11-server`; если назвать её у каждого
по-своему, `x11vnc` не сможет сослаться ни на одну.

Имя расширения тоже считается способностью — на него ссылаются `conflicts`.

### `requires`

Способности, без которых расширение не работает. **Каждая обязана кем-то
предоставляться** — это проверяет валидатор.

Заполняется по коду, а не по интуиции. Пример: `x11vnc` требует `x11-server`
и `display-manager`, потому что `x11vnc@.service` прицепляется к `DISPLAY=:0`
и несёт `Requires=display-manager.service`, а обёртка `run-x11vnc.sh` ищет
X authority в каталогах, которые заводит дисплей-менеджер. Требование
**родовое**, и это верно: конкретный путь в юните не зашит — под GDM и
LightDM он разный, а под самим LightDM зависит от его конфига, — поэтому
различать в манифесте «какой именно менеджер» значило бы зафиксировать ось,
которая ответа не определяет.

Ровно это отношение сегодня держится только порядком слоёв в четырёх VMFILE:
поменяй местами `gnome` и `x11vnc` — сборка пройдёт, а VNC окажется не к чему
прицепить.

### `conflicts`

Имена расширений, с которыми это несовместимо. **Конфликты обязаны быть
симметричны**: если `gnome` конфликтует с `lxde`, то `lxde` обязан объявить
конфликт с `gnome`. Односторонний конфликт — это конфликт, о котором узнает
только один из двух авторов.

Версионного решателя нет и не планируется: грамматика Debian
(`Provides`/`Depends`/`Conflicts`) заимствована, механизм — нет.

**И решателя порядка тоже нет**, хотя написать «порядок считается
топологической сортировкой» было бы приятнее. Валидатор ищет циклы в графе
`requires` → `provides` и требует симметрии конфликтов — то есть ловит
неверно **объявленное**. Сборка эти три поля не читает вовсе: `EXTENSION`
применяет расширения ровно в том порядке, в каком они написаны в VMFILE,
и два конфликтующих десктопа подряд она поставит молча.

## Словарь способностей

Полный список того, что объявлено сегодня, и на чём это основано.

| Способность | Кто даёт | Откуда выведено |
|---|---|---|
| `x11-server` | `gnome`, `xfce4`, `lxde` | `apt-get install ... xorg xinput` (`install.sh:21` у всех трёх) |
| `display-manager` | `gnome`, `xfce4`, `lxde` | `systemctl enable gdm3`/`lightdm` + `set-default graphical.target` (`install.sh:38-39`) |
| `desktop-session` | `gnome`, `xfce4`, `lxde` | `task-gnome-desktop` / `xfce4 xfce4-goodies` / `lxde` |
| `container-runtime` | `docker` | Docker CE из apt-репозитория Docker |
| `kiosk-browser` | `kiosk`, `chromium-kiosk` | обе реализации автостартом забирают экран |
| `remote-desktop` | `x11vnc`, `vino-vnc` | `x11vnc@.service` на `rfbport` из `X11VNC_PORT`; у `vino-vnc` — `gsettings` `org.gnome.Vino` и автозапуск XDG (умолчание у обоих 5900) |
| `web-ide` | `code-server` | code-server на порту `CODE_SERVER_PORT` домена `code-server` (умолчание 9001) |
| `gpu-driver-nvidia` | `nvidia` | проприетарный драйвер + blacklist nouveau |
| `nocloud-cidata-seed` | `nocloud-cidata` | `/etc/cloud/cloud.cfg.d/zz-bisquite-nocloud.cfg` с `fs_label: cidata` |
| `network-manager` | `network-manager` | пакеты `network-manager` и `wpasupplicant`, включённый `NetworkManager.service` |
| `jetson-monitor` | `jetson-stats` | `jtop` из PyPI плюс `jtop.service`; данные идут из `tegrastats` и sysfs Tegra, а не из NVML |
| `cuda` | `cuda-toolkit` | `cuda-toolkit-12-6` из `jetson/common`, `nvcc` в `/usr/local/cuda/bin` |
| `tensorrt` | `tensorrt` | мета-пакет `tensorrt` плюс `python3-libnvinfer` и `nvidia-l4t-dla-compiler` |
| `web-remote-desktop` | `selkies` | Selkies 2.0 (AppImage) на существующем X-дисплее, WebSocket на `127.0.0.1:8080`; `selkies@.service` от пользователя сессии |
| `web-browser` | `firefox` | `firefox` из `packages.mozilla.org` с пином 1000 поверх транзитного пакета снапа |
| `teleport-node` | `teleport-agent` | `/usr/local/bin/teleport`, `teleport.service` из `bisquite-teleport join`; приложения — из `/etc/bisquite/teleport/apps.d` |
| `l4t-boot-verify` | `l4t-boot-verify` | `bisquite-l4t-boot-verify.service` — `nvbootctrl verify` на каждой загрузке; вендорская `nv-l4t-bootloader-config` заглушена |
| `gmsl2-camera` | `sensing-gmsl2-camera` | ядро и модули Sensing, DTB-оверлей, запуск камер на каждой загрузке (`modprobe.d`, правило udev, `bisquite-sensing-clock.service`) |
| `gstreamer` | `l4t-gstreamer` | `gstreamer1.0-tools` и плагины base/good/bad/ugly/libav, RTSP-сервер, `gstreamer1.0-nice`, `python3-gst-1.0` |
| `hw-video-codec` | `l4t-gstreamer` | `nvidia-l4t-gstreamer` версии `nvidia-l4t-core`: `nvv4l2h264enc`/`h265enc`/`decoder`, `nvvidconv` |
| `opencv` | `l4t-opencv` | `libopencv` и `libopencv-python` 4.8.0 от NVIDIA, с GStreamer, без CUDA |
| `pytorch` | `l4t-pytorch` | колесо torch nv24.08 с CDN NVIDIA, torchvision из исходников с CUDA |
| `tensorflow` | `l4t-tensorflow` | колесо tensorflow nv24.08 с CDN NVIDIA |

Кто чего требует:

| Расширение | `requires` | Откуда выведено |
|---|---|---|
| `x11vnc` | `x11-server`, `display-manager` | `x11vnc@.service` — `Requires=display-manager.service` и `DISPLAY=:0`. Путь к X authority в юните НЕ зашит: он разный под LightDM и GDM, и обёртка ищет его в рантайме |
| `kiosk` | `x11-server`, `display-manager` | `install.sh` ставит клиентские утилиты X, но **не** `xorg`; `kiosk-chromium@.service` — `DISPLAY=:0`; `configure-kiosk.service:3` — `After=graphical.target … display-manager.service`. Путь к X authority в юните, как и у `x11vnc`, НЕ зашит — его ищет обёртка `run-kiosk.sh` |
| `vino-vnc` | `x11-server`, `display-manager` | vino живёт ВНУТРИ пользовательской сессии: `configure.sh` пишет `gsettings` в dconf пользователя и кладёт автозапуск в его `~/.config/autostart`, а сессию открывает дисплей-менеджер. Системного юнита и поиска X authority здесь нет вовсе — этим он и отличается от `x11vnc` |
| `selkies` | `x11-server`, `display-manager` | `run-selkies.sh` снимает экран существующего X-дисплея и ждёт сессию пользователя; без автологина показывать нечего |
| `l4t-pytorch` | `cuda` | `install.sh` отказывает без `/usr/local/cuda/bin/nvcc`: им собирается torchvision с `FORCE_CUDA=1`; сам torch грузит `libcublas`, `libcufft` и прочие библиотеки тулкита |
| `l4t-tensorflow` | `cuda` | `install.sh` отказывает без `/usr/local/cuda/lib64`: TF грузит библиотеки тулкита |
| остальные | — | ничего не требуют по коду |

Конфликты:

| Пара | Причина |
|---|---|
| `gnome` ↔ `xfce4` ↔ `lxde` | каждый ставит свой дисплей-менеджер и делает его системным `display-manager.service` |
| `kiosk` ↔ `chromium-kiosk` | оба автостартом на `graphical.target` разворачивают полноэкранный браузер на одном месте |

Оговорка про честность вывода: первые две пары измерены по коду, третья —
рассуждение, а не замер. Ни один VMFILE не ставит `kiosk` и `chromium-kiosk`
вместе, так что проверить конфликт на практике не на чем.

### Docker: одно расширение, и почему их было два

Расширений было два — `docker` через `get.docker.com` и `docker-ce` через
apt-репозиторий, — на том основании, что старым системам нужен скрипт, а
новым репозиторий. Замер 2026-09-03 опроверг это дважды, и они слиты.

**Скрипт ставит из того же репозитория.** `get.docker.com` пишет однострочный
`.list` на `download.docker.com` вместо deb822 (`getdocker.sh:596,606`).
Второго источника пакетов никогда не существовало, значит и противопоставления
не было.

**На bionic скрипт ломает сборку.** `version_gte()` при пустом `VERSION`
возвращает истину всегда (`getdocker.sh:230-234`), поэтому гейт
`version_gte "28.2"` пропускает `docker-model-plugin` в список безусловно —
а в репозитории bionic его нет ни для amd64, ни для arm64. При `set -e` это
фатально.

**Довод про apt тоже неверен.** deb822 с `Signed-By: <путь>` работает начиная
с apt 1.6.17 (замерено в `ubuntu:18.04`); apt ≥ 2.4 нужен только встроенному
ключу, которым никто не пользуется.

Что осталось верным: репозиторий для bionic **заморожен** — Release от
13.06.2023, `docker-ce 24.0.2` против 29.7.2 у noble. Но это потолок цели,
а не выбор способа установки, и расширение говорит об этом вслух.

Разбор целиком — в `extensions/debian/docker/README.md`.

## Раскладка в госте и общий код `lib/`

Всё, что ставят расширения, лежит под одним именем — по назначению:

| Что | Где |
|---|---|
| настройки расширений (`config`, `apps.d`) | `/etc/bisquite/<домен>/` |
| код и данные расширения: `run-*.sh`, `configure.sh`, картинки | `/opt/bisquite/<имя>/` |
| общий код источника (`get_cloud_user.sh`, `bisquite-desktop`) | `/opt/bisquite/lib/<sha256>/`; расширение видит его как `/opt/bisquite/<имя>/lib` |
| состояние | `/var/lib/bisquite/<имя>/` |
| команды для человека | `/usr/local/sbin/bisquite-*` |

`/etc` — только настройки: его держат в etckeeper, бэкапят и сравнивают, а
в `/opt/<пакет>` по FHS живёт стороннее ПО целиком.

**Что переехало с раскладкой 2:**

| Было | Стало |
|---|---|
| `/opt/vmsetup/<имя>/` | `/opt/bisquite/<имя>/` |
| `/opt/vmsetup/{code-server,kiosk,chromium-kiosk}/config.yaml` | `/etc/bisquite/<имя>/config` (`KEY=VALUE`; до библиотеки настроек — `config.yaml`) |
| копии `get_cloud_user.sh`, `bisquite-desktop` в каталогах расширений | `lib/` источника, ссылка `/opt/bisquite/<имя>/lib` |
| `/usr/local/lib/bisquite-desktop/get_cloud_user.sh` | не нужен: `bisquite-desktop` — ссылка в `lib/` |
| `/var/lib/bisquite-teleport/` | `/var/lib/bisquite/teleport/` |

Уже записанные устройства не мигрируют: образы пересобираются.

### `lib/` — контракт доставки

Каталог `lib/` в корне источника — не место хранения исходника, из которого
что-то копируют, а то, что **сборка доставляет в гостя**:

1. на хосте считается sha256 дерева `lib/` (пути и содержимое по порядку);
2. дерево кладётся в `/opt/bisquite/lib/<sha256>/`, если такого в сборке ещё
   нет, скриптам ставится `+x`;
3. рядом с каталогом расширения ставится ссылка
   `/opt/bisquite/<имя>/lib → /opt/bisquite/lib/<sha256>`.

Отсюда правило вызова, одно на все расширения:

- в `install.sh` и `configure.sh` — `"$SCRIPT_DIR/lib/get_cloud_user.sh"`,
  где `SCRIPT_DIR` — каталог самого скрипта;
- в юнитах — `/opt/bisquite/<имя>/lib/…`.

**Почему по отпечатку, а не одним `/opt/bisquite/lib`.** Цепочки `FROM` —
обычное дело (`debian12:base` → `gnome`, `jetson-orin-base` →
`jetson-orin-robot`). Общий каталог перезаписывался бы дочерним слоем:
расширения базы на первой загрузке звали бы `lib` другого коммита, а
удалённые из источника файлы оставались бы висеть. С отпечатком расширение
навсегда видит тот `lib`, с которым его ставили; одинаковый `lib` лежит
в образе одной копией, а по имени каталога видно, какой именно.

Что кладётся в `lib/`: только то, что нужно **нескольким** расширениям и
не зависит ни от одного из них. Всё, что в `lib/`, уезжает в каждый образ
с расширением этого источника, поэтому место в нём не бесплатное.

### Почему копий больше нет

До раскладки 2 до гостя доезжал только каталог одного расширения, и
общий код лежал в каталогах потребителей копиями: двенадцать
`get_cloud_user.sh`, четыре `bisquite-desktop`. Копии генерировал и сверял
отдельный инструмент со списками получателей. Он работал, но цена копий
проявилась дважды: к моменту наведения порядка восемь копий
`get_cloud_user.sh` разошлись оформлением, а позже источник отстал от копий
поведением (`fallback_user()` был в копиях и не был в `lib/`), и
«синхронизация» откатила бы запасной путь в десяти расширениях разом.
Доставка `lib/` сборкой убирает сам предмет сверки: копий нет, расходиться
нечему. Историю копий хранит git.

## Новое расширение: каркас

```bash
tools/new-extension.sh <имя> [--family deb|openwrt] [--arch amd64,arm64]
                             [--no-firstboot] [--no-shared-user]
```

Кладёт скелет с манифестом `layout: 2`, юнитом
`ExecStart=/opt/bisquite/<имя>/configure.sh` и — если расширению нужен общий
resolve пользователя cloud-init — вызовом `"$SCRIPT_DIR/lib/get_cloud_user.sh"`
в `configure.sh`. Регистрировать ничего не надо: `lib/` доставляет сборка.

Сгенерированное проходит `tools/check.sh` сразу, но манифест намеренно
пустой: `provides` и `requires` заполняет автор, **выводя их из кода** —
из того, что `install.sh` реально ставит и что требуют юниты. Заполненные
за автора способности выглядели бы обдуманными и пережили бы ревью.

Каркас не создаёт `configure.sh` при `--no-firstboot`: расширение без фазы
первой загрузки — законный случай (так устроен `nvidia`), и пустой скрипт
там был бы обещанием, которого никто не держит.

### `bs extension new` — и когда он вместо скрипта

Команда в bisquite появилась и кладёт ту же раскладку:

```bash
bs extension new <имя> [--path DIR] [--family deb] [--arch amd64,arm64]
                       [--no-firstboot]
```

Разница одна, и она решает выбор. `bs extension new` **отказывается писать
внутрь кеша источников** — `bs extension sync` заменяет каталог источника
целиком, и написанное там исчезло бы с первой синхронизацией; отказ называет
это прямо и предлагает объявить свой каталог источником `type: path`.
Общий код у него тот же: `$SCRIPT_DIR/lib/` — это `lib/` корня того
источника, где расширение найдётся.

Для расширения **в этом дереве** каркас удобнее класть `tools/new-extension.sh`
(он знает семейства и шаблон README этого репозитория), а `bs extension new` —
для своего источника на стороне.

## Проверки

```bash
tools/check.sh                # всё сразу: валидатор и тесты библиотеки настроек
tools/validate-extensions.py  # манифесты, схемы, обращение с файлами настроек
tools/test-conf.sh            # lib/bisquite-conf и домены на подменённом корне
```

`tools/validate-extensions.py` проверяет:

1. у каждого каталога с `install.sh` есть `extension.yaml` (каталоги без
   `install.sh` печатаются списком «пропущено»);
2. набор полей ровно тот, что описан выше — лишнее поле роняет проверку;
3. `name` совпадает с каталогом, `version` — semver, `layout` — ровно `2`,
   `family` из известного набора, `arch` непустой, `phase` из
   `{build, firstboot}`;
4. каждая способность из `requires` кем-то предоставляется;
5. в графе `requires` → `provides` нет циклов;
6. `conflicts` симметричны и ссылаются на существующие расширения;
7. настройки — через библиотеку: схема (`knobs`, `lib/knobs/<домен>`) разбирается
   самой `lib/bisquite-conf`, `knobs` расширения регистрирует его `install.sh`,
   скрипты не пишут `/etc/bisquite/<домен>/config` мимо библиотеки и не исполняют
   его (`source`/`.`), хуки `.apply`/`.secret` не зовут блокирующий
   `systemctl start|restart`.

`tools/test-conf.sh` проверяет библиотеку на подменённом корне
(`BISQUITE_CONF_ROOT`): каждый тип, шаблон, отказ неизвестного ключа,
сохранение комментариев, повторный `conf_init`, `secret:hook` (успех и отказ,
при котором не пишется ничего), stdin, неисполнение `$(…)`, миграцию YAML,
пропуск хука во время загрузки — и совместимость разбора с systemd: прогоном
файла через `systemd-run --user -p EnvironmentFile=`, если он доступен, иначе
сверкой с эталоном, снятым с systemd 257. Затем — домены: настоящие схемы,
хуки и скрипты расширений, разложенные так, как их кладёт сборка.

Предупреждением (без падения) отмечается рассогласование `phase` с наличием
`configure.sh`.

Нужен Python 3 с PyYAML и bash.

### Где это выполняется само

Проверки жили ручными скриптами, и это была не мелочь: ровно так и разошлись
восемь копий `get_cloud_user.sh` — расхождение ничем не ловилось на пути
в репозиторий. Проверка, которую надо не забыть запустить, — не проверка.

Теперь `tools/check.sh` гоняет GitHub Actions: `.github/workflows/check.yml`,
на каждый push в любую ветку и на каждый pull request в `main`. Один job, одна
команда — та же, что запускают руками, чтобы CI и локальный прогон не
разъезжались.

Три вещи, которые стоит знать про этот workflow:

- **`runs-on: ubuntu-latest`, а не `[self-hosted, linux, x64]`,** как в основном
  репозитории. Там раннер нужен ради docker, Nuitka и сборки деба; здесь из
  зависимостей — bash и Python с PyYAML. Плюс job, приколоченный к раннеру,
  которого у репозитория может не оказаться, не падает, а висит в очереди
  навсегда, и это хуже красного CI.
- **PyYAML ставится явно**, хотя на образе раннера он есть. Наличие — деталь
  образа, а не контракт; без явной установки обновление образа выключило бы
  половину проверки. Отказ был бы внятным (валидатор отвечает кодом 2 и
  сообщением про PyYAML), но проверять он бы перестал.
- **`check.sh` доводит до конца все проверки**, даже если первая упала. Сегодня
  проверка одна, но точка входа устроена так, чтобы следующая встала рядом и
  ошибки обеих были видны в одном прогоне CI.

Локально перед пушем — та же одна команда:

```bash
tools/check.sh
```

Что печатается при нарушении:

```
$ tools/check.sh          # неполный манифест
== validate-extensions ==
FAIL: extensions/debian/x11vnc: нет обязательного поля `layout`
FAIL: extensions/debian/x11vnc: `version: 1.0` не semver (ожидается X.Y.Z)
FAIL: extensions/debian/x11vnc: `requires: nonexistent-capability` — эту способность никто не предоставляет

валидация не прошла: ошибок 3
проверки не прошли      # код возврата 1
```

## Как добавить расширение

1. Создай `extensions/<группа>/<имя>/` с `install.sh` — идемпотентным,
   запускаемым от root внутри гостя, читающим параметры из окружения.
   Настройки, которые меняют на устройстве, объяви схемой `knobs`, а файл
   заводи вызовом `conf_init` (см. «Библиотека настроек `bisquite-conf`»).
2. Нужна донастройка на устройстве — добавь `configure.sh` и
   `configure-<имя>.service`, а `install.sh` пусть кладёт юнит в
   `/etc/systemd/system/` и делает `systemctl enable`. Учитывай, что путь
   в `ExecStart` — это путь **в госте**: `/opt/bisquite/<имя>/configure.sh`.
   Настройки устройства клади в `/etc/bisquite/<имя>/`, состояние —
   в `/var/lib/bisquite/<имя>/`.
3. Нужен `get_cloud_user.sh` или другой общий код — зови его как
   `"$SCRIPT_DIR/lib/get_cloud_user.sh"` и не копируй в каталог расширения.
4. Напиши `extension.yaml` с `layout: 2`. `provides` и `requires` выводи из кода: что
   реально ставится и на что реально опираются юниты. Не изобретай
   зависимостей, которых в коде нет.
5. Прогони `tools/check.sh`.
6. Напиши `README.md` расширения: что делает в каждой фазе, **таблицу
   параметров** (переменных окружения), которые читает `install.sh`, и
   подключение через `EXTENSION`. Таблица параметров — не
   вежливость: VMFILE передаёт их именами скрипта, и другого справочника
   у автора VMFILE нет.
7. Поставь тег репозитория — он же `ref` источника в `extensions.yaml`.

## Что осталось незакрытым

Перечислено, чтобы не выглядело сделанным.

- **Дефект «одна способность двумя механизмами» не закрыт.** Docker ставится
  расширением примерно в половине случаев, в остальных — голым `INSTALL`
  в VMFILE. В один граф зависимостей эти два способа не попадают, поэтому
  топологическая сортировка тут не поможет. Закрывается только приведением
  примеров к единообразию, то есть правками в основном репозитории.
- **Требования к базовому образу манифестом не выражаются.** Расширениям,
  которые узнают пользователя cloud-init через `lib/get_cloud_user.sh`, нужны
  в госте `cloud-init` и `yq` (без них — запасной путь по первой учётке
  с uid ≥ 1000), и ставит их базовый VMFILE, а не расширение. Поля для
  этого в наборе нет и придумывать его в обход спеки не стали; проверить,
  что база их даёт, сегодня нечем.
- **Графа зависимостей никто не исполняет.** Резолвер и отказ по архитектуре
  появились, а топологической сортировки и отказа по конфликту нет:
  `provides`/`requires`/`conflicts` bisquite только печатает. Порядок слоёв
  по-прежнему держится вниманием автора VMFILE — поменяй местами `gnome`
  и `x11vnc`, и сборка пройдёт зелёной.
- **`arch` у `chromium-kiosk` объявлен, но не проверен.** Пакет тянется из
  стороннего репозитория `repository.salamek.cz` (suite `all`); что там есть
  под arm64, не замерялось. На сборках bisquite подтверждён только amd64.
- **Фаза `firstboot` в одиночку не работает.** Расширение, объявившее только
  её, даёт отказ: ветки под такой случай нет, потому что в дереве нет ни
  одного такого расширения. Практического ограничения это сегодня не создаёт,
  но объявление в манифесте выглядит шире, чем реализация.
- **Разбор `bisquite-conf` сверяется с systemd одной версии.** Эталон снят
  с systemd 257; на Jetson с Ubuntu 22.04 стоит 249, где комментарий,
  оканчивающийся `\`, съедает следующую строку (до v254). Писатель таких
  комментариев не создаёт, но руками их поставить можно.
- **Пароль x11vnc проходит через argv `x11vnc -storepasswd`** на время
  короткого вызова хука: у `-storepasswd` нет чтения со stdin. Из argv самой
  `bisquite-conf` секрет убран (`KEY=-`).
