name: docker
version: 1.0.0
description: Docker CE — engine, CLI, buildx и compose plugin.
os:
  - debian: ">=11"
  - ubuntu: ">=22.04"
depends: []
params:
  DOCKER_USERS:
    default: debian
    description: Пользователи, добавляемые в группу docker (через пробел).
