# x11vnc

VNC-доступ к рабочему столу X11 для пользователя, созданного cloud-init.
Сервер поднимается системным юнитом-шаблоном `x11vnc@<пользователь>.service`
и по умолчанию слушает только петлю.

> **С 3.0.0 — раскладка 2.** Каталог расширения в госте —
> `/opt/bisquite/x11vnc/` (был `/opt/vmsetup/x11vnc/`), в том числе в `ExecStart`
> обоих юнитов; `lib/get_cloud_user.sh` — ссылкой на общий код источника,
> а не копией. Нужен bisquite с поддержкой `layout: 2`.

> **С 4.0.0 — библиотека настроек.** Файл `/etc/bisquite/x11vnc/config` —
> домен `x11vnc` библиотеки `bisquite-conf` (схема `knobs` рядом): установка
> его не переписывает, значения проверяются (`X11VNC_LISTEN` — только
> `localhost` или `all`), пароль ставится командой
> `bisquite-conf set x11vnc X11VNC_PASSWORD=-`. Правка `sed`-ом больше не
> нужна — и не проверяется.

**С 2.0.0 файл параметров — `/etc/bisquite/x11vnc/config`** (был `/etc/default/bisquite-x11vnc`).
Прежний путь не читается; если файл по нему остался в базовом образе,
установка его удаляет.

## Манифест

| Поле | Значение |
|---|---|
| `phase` | `[build, firstboot]` |
| `provides` | `remote-desktop` |
| `requires` | `x11-server`, `display-manager` |
| `conflicts` | — |

`build` — `install.sh` ставит пакеты, кладёт юниты и файл параметров;
`firstboot` — `configure.sh` включает `x11vnc@<пользователь>.service`, а имя
пользователя приходит из cloud-init уже на устройстве.

Требование родовое — «любой X-сервер, любой дисплей-менеджер», — и это
верно: путь к X authority в юните не зашит, а ищется в рантайме, а он разный
под LightDM и GDM и зависит от конфига, а не от имени пакета. Различать
в манифесте «какой именно менеджер» значило бы зафиксировать ось, которая
ответа не определяет.

`conflicts` с `vino-vnc` **не объявлен** намеренно — разбор в README
`vino-vnc`, раздел «Соседство с x11vnc»: мешают друг другу не расширения,
а их умолчания (оба берут порт 5900), и разводится это параметром.

## Порядок в VMFILE — несущий, и несущая строка в нём одна

```vmfile
EXTENSION xfce4
EXTENSION x11vnc X11VNC_PORT=5901
```

1. **`yq` больше не условие работы.** `configure.sh` раньше начинал
   с `command -v yq` и при его отсутствии выходил кодом 1 — при том что сам
   `yq` не вызывал ни разу. Нужен он только внутри `lib/get_cloud_user.sh`,
   и там его отсутствие — **не отказ, а переход к запасному пути**: «первая
   учётка с uid 1000..65533 и домашним каталогом». Проверка снята, и сервер
   теперь настраивается и без `yq`.

   Ставить `EXTENSION yq` выше по-прежнему осмысленно, но по другой причине
   и с другой ценой: **с `yq` настроят именно cloud-init-пользователя,
   без `yq` — первую подходящую учётку**, а на образе с вендорской учёткой
   это может быть не тот человек. Запасной путь об этом говорит вслух —
   строкой в stderr `Note: cloud-init user unavailable; falling back to …`.
2. **Десктоп — выше.** Он даёт `x11-server` и `display-manager`
   (`gnome`, `xfce4` или `lxde`). Топологической сортировки у резолвера нет:
   `requires` печатается, но порядок держится этой строкой.

## Нужна сессия пользователя

x11vnc подключается к X-сессии пользователя, а не к экрану входа. С 2.0.0
десктопных расширений автологин по умолчанию выключен: без
`DESKTOP_AUTOLOGIN=1` до входа человека сессии нет, и служба перезапускается
с «не нашёл X authority». Профиль «рабочий стол всегда на экране»:

```vmfile
FIRSTBOOT_COMMAND "bisquite-desktop set DESKTOP_AUTOLOGIN=1 DESKTOP_DISABLE_SCREEN_LOCK=1 DESKTOP_DISABLE_SCREEN_BLANK=1"
```

## Параметры сборки

Задаются переменными окружения. В инструкции `EXTENSION` они пишутся
`КЛЮЧ=ЗНАЧЕНИЕ` и уезжают в `install.sh` как есть:

```vmfile
EXTENSION x11vnc X11VNC_PORT=5901 X11VNC_PASSWORD=секрет
```

| Переменная | Умолчание | Что делает |
|---|---|---|
| `X11VNC_PORT` | `5900` | порт RFB (`-rfbport`) |
| `X11VNC_DISPLAY` | `:0` | дисплей X (`-display`) |
| `X11VNC_PASSWORD` | пусто | пароль; без него сервер поднимается с `-nopw` |
| `X11VNC_LISTEN` | `localhost` | `localhost` — только петля (`-localhost`), `all` — все интерфейсы |

Имена, типы и умолчания объявлены один раз — в схеме `knobs`. `install.sh`
отдаёт параметры библиотеке (`conf_init x11vnc … --env`): она проверяет их,
создаёт `/etc/bisquite/x11vnc/config` один раз и дальше **не переписывает** —
повторная установка оставляет правки оператора, а параметры VMFILE ложатся
поверх. Опечатка (`X11VNC_LISTEN=al`, `X11VNC_PORT=http`) роняет сборку.

Пароль в файл не пишется: это ключ `secret:hook`. Хук `knobs.secret` делает
`x11vnc -storepasswd` в `/etc/x11vnc/passwd` (формат `-rfbauth`, `0600`), а
в файл окружения уезжает только путь (`X11VNC_PASSFILE`). Хук отказал — не
записано ничего и сборка падает: сервер без `X11VNC_PASSFILE` поднимается
с `-nopw`, и образ выглядел бы защищённым.

**`X11VNC_LISTEN` — переключатель, а не адрес.** Обёртка сравнивает его ровно
со строкой `localhost` и либо добавляет `-localhost`, либо не добавляет
ничего; привязать сервер к одному конкретному адресу этим параметром нельзя.
До 4.0.0 «все интерфейсы» означало любое значение, кроме `localhost`; теперь
схема принимает только `all`.

Значение с пробелами берётся в кавычки — аргумент разбирается `shlex`,
и кавычки снимаются до попадания в окружение:

```vmfile
EXTENSION x11vnc X11VNC_PASSWORD="two words"
```

**Умолчание `localhost` — смена поведения.** Раньше сервер слушал все
интерфейсы и всегда без пароля, а compose-примеры ставят `firewall: false`:
рабочий стол оказывался открыт всей сети из одной строки VMFILE, которую
никто не писал специально. Позиция проекта — только петля, доступ через
SSH-туннель:

```bash
ssh -L 5900:localhost:5900 пользователь@адрес
```

## Параметры времени выполнения

Параметры живут в **`/etc/bisquite/x11vnc/config`** — это `EnvironmentFile`
юнита `x11vnc@.service`, а не копия сборочных значений. Менять их на живой
машине можно и пересобирать образ для этого не нужно — командой
`bisquite-conf`, которая проверяет значение, пишет файл атомарно и перезапускает
включённый экземпляр (хук `knobs.apply`, без ожидания).

```console
$ bisquite-conf show x11vnc
КЛЮЧ               ЗНАЧЕНИЕ    ОТКУДА     ТИП — ОПИСАНИЕ
X11VNC_PORT        5900        файл       port — порт RFB
X11VNC_LISTEN      localhost   файл       enum:localhost,all — …
X11VNC_PASSWORD    <хук>       —          secret:hook — пароль VNC; …
X11VNC_PASSFILE    —           умолчание  ro:path — файл пароля для -rfbauth; …
```

Переключить прослушивание с петли на все интерфейсы:

```bash
# 1. пароль — со stdin, чтобы не светить его в argv и истории;
#    хук кладёт /etc/x11vnc/passwd (0600, владелец — пользователь сервера)
#    и записывает X11VNC_PASSFILE
echo 'пароль' | sudo bisquite-conf set x11vnc X11VNC_PASSWORD=-

# 2. адрес; хук перезапускает включённый x11vnc@<пользователь>
sudo bisquite-conf set x11vnc X11VNC_LISTEN=all

# 3. убедиться, что слушает то, что ожидалось
ss -ltnp | grep 5900
```

Вернуть обратно — `X11VNC_LISTEN=localhost`; снять пароль — пустым значением
(`X11VNC_PASSWORD=`), хук удалит файл и очистит `X11VNC_PASSFILE`. Опечатка —
ненулевой код и текст о допустимых значениях, файл не тронут. Во время
загрузки (команды первой загрузки манифеста) `set` только пишет: сервер
прочтёт файл при своём старте. `configure-x11vnc.service` перезапускать
бесполезно: он только находит пользователя и включает экземпляр.

### Что именно произойдёт при `X11VNC_LISTEN=all`

Сказать это прямо дешевле, чем узнать потом:

- **рабочий стол становится доступен всем, кто дотянулся до порта** — уже
  открытой сессии, с правами вошедшего пользователя, включая его терминалы
  и сохранённые пароли в браузере;
- **трафик не шифруется.** RFB открытым текстом: нажатия клавиш (а значит,
  и пароль `sudo`) идут по сети как есть;
- **пароль VNC слабый по устройству протокола.** Аутентификация RFB (тип 2)
  кладёт в ключ DES ровно восемь байт; девятый символ и дальше
  отбрасываются, перебор по такому ключу дёшев;
- **следа не останется.** Подключение не пишется ни в `wtmp`, ни в журнал
  сессии — только строка в journal самого `x11vnc@`;
- **compose-примеры ставят `firewall: false`**, то есть межсетевого экрана
  перед этим портом может не быть вовсе.

Поэтому штатный путь — оставить `localhost` и ходить SSH-туннелем: так
шифрование и аутентификацию даёт SSH, а VNC остаётся локальным сервисом.
Открывать в сеть осмысленно в изолированном сегменте и с паролем.

## Чего расширение не делает

- **не поднимает десктоп и не выключает Wayland** — это работа десктопного
  расширения (`gnome` пишет `WaylandEnable=false`);
- **не открывает порт в межсетевом экране** и не настраивает туннель;
- **не ставит `yq`** — и больше в нём не нуждается (см. «Порядок в VMFILE»);
- **не шифрует трафик** — ни `-ssl`, ни `stunnel` здесь нет.

**Каталог `/opt/bisquite/x11vnc` обязан остаться в образе.** Оба юнита зовут
скрипты по этому пути в рантайме (`configure.sh` и `run-x11vnc.sh`), а не
копируют их куда-то «к себе». Инструкция `EXTENSION` кладёт каталог именно
туда, так что штатным путём это само собой; но слой, который вычистит
`/opt/bisquite`, сломает VNC — причём не на сборке, а на первой загрузке.
Сам `install.sh` при этом свои файлы ищет **рядом с собой**
(`$SCRIPT_DIR`), а не по зашитому `/opt/bisquite/x11vnc/...`: он отвечает на
вопрос «файл приехал рядом со мной?», а не «раскладка всё ещё такая?».
**Юниты же зовут скрипты по `/opt/bisquite/x11vnc/`**, поэтому каталог
расширения обязан лежать именно там — так его кладёт `EXTENSION`.

**Файл пароля `/etc/x11vnc/passwd` создаётся с правами `0600`.** Формат
`-rfbauth` — не хеш, а обратимо зашифрованный пароль (DES с фиксированным
ключом), поэтому прежние `0644` означали, что пароль VNC читает любая
локальная учётная запись — и читает ровно тогда, когда пароль задали, то
есть когда сервер выставлен в сеть.

**Владельца файл получает на первой загрузке, а не на сборке.** Сервер
работает юнитом `x11vnc@<пользователь>` с `User=%i`, то есть пароль читает
не root, а учётка, которой на сборке ещё нет (её создаёт cloud-init).
Поэтому `install.sh` ставит только права, а `chown` на найденного
пользователя делает `configure.sh`. Пара обязательна: `0600 root:root`
означало бы, что обёртка файл не прочтёт, и раньше она в этом случае тихо
поднимала сервер с `-nopw` — права, закрывающие пароль, открыли бы рабочий
стол. Теперь нечитаемый файл пароля — **отказ обёртки** со строкой в журнале
и подсказкой `chown`.

Правка не отменяет последствий: на уже прошитых устройствах файл был `0644`,
и пароль оттуда уже мог быть прочитан. Новый образ лечит только новые
устройства, остальным нужна **ротация пароля** — отдельное действие.

## Что делает

**Сборка** (`install.sh`)

- ставит `x11vnc`, `xauth`, `x11-utils`;
- проверяет, что рядом с ним лежат все пять своих файлов
  (`x11vnc@.service`, `configure-x11vnc.service`, `run-x11vnc.sh`,
  `configure.sh`, `lib/get_cloud_user.sh`); **отсутствие любого — отказ сборки**,
  а не предупреждение: раньше сборка оставалась зелёной, юнита в образе не
  было, и узнавал об этом тот, кто включил плату без монитора;
- кладёт `/etc/systemd/system/x11vnc@.service`
  и `/etc/systemd/system/configure-x11vnc.service`;
- регистрирует схему `knobs` и хуки в `/opt/bisquite/knobs/x11vnc*`, создаёт
  `/etc/bisquite/x11vnc/config` (если его нет), проверяет и записывает параметры
  VMFILE, а если задан пароль — хук кладёт `/etc/x11vnc/passwd` правами `0600`;
  **не записавшийся файл пароля — тоже отказ**, потому что без него сервер
  поднимается с `-nopw`;
- включает `configure-x11vnc.service`;
- предупреждает, если `X11VNC_LISTEN` не `localhost`, а пароль не задан.

`xauth` и `x11-utils` ставятся **явно**: обёртке нужен `xdpyinfo`, а у пакета
`x11vnc` он лишь в `Recommends`. Раньше они приезжали только потому, что
десктопное расширение ставило `xorg`, — то есть работа зависела от порядка
слоёв, а не от объявленных зависимостей.

**Первая загрузка** (`configure.sh`, юнит `configure-x11vnc.service`)

- отказывает сразу, если рядом нет исполняемого `lib/get_cloud_user.sh`
  (проверки `yq` здесь больше нет — см. «Порядок в VMFILE»);
- ждёт пользователя cloud-init — до 120 секунд, 40 попыток по 3 с;
- передаёт файл пароля найденному пользователю (`chown` плюс `0600`) —
  без этого сервер его не прочитает;
- **гасит прежние экземпляры** `x11vnc@*`, включённые в
  `graphical.target.wants/`: раньше после смены пользователя оставались оба,
  и второй сервер уходил в цикл рестарта, не заняв порт. Устройство, где два
  экземпляра уже включены, новым образом не лечится — только перезаписью
  носителя или `systemctl disable` руками;
- включает и запускает `x11vnc@<пользователь>.service`; **отказ запуска —
  это отказ юнита** (код 1 и строка в журнале), а не тишина: раньше обе
  команды глушились, и немой цикл рестарта выглядел как успех.

Юнит ждёт пользователя **циклом**, а не упорядочиванием: в отличие
от `configure-vino-vnc.service` он не стоит `After=cloud-final.service`,
а висит на `graphical.target` после дисплей-менеджера. Практически это
означает, что запас по времени — те самые 120 секунд, и на очень медленном
cloud-init их может не хватить; в журнале это видно строкой «Timeout waiting
for cloud-init user».

Сам сервер (`x11vnc@.service`) перед запуском ждёт сокет X до 60 секунд
(30 попыток по 2 с), а при падении перезапускается через 5 секунд
(`Restart=on-failure`). Аргументы сервера собирает обёртка:
`-forever -loop -noxdamage -repeat -shared`, плюс порт, дисплей, authority
и либо `-localhost`, либо ничего, и либо `-rfbauth`, либо `-nopw`.

## Как находится X authority

Путь к authority **не зашит в юните**, а ищется в рантайме обёрткой
`run-x11vnc.sh`. Прежний хардкод

```
-auth /var/run/lightdm/%i/:0
```

не работал **ни под одним** дисплей-менеджером. Это склейка двух разных схем
LightDM (проверено по исходникам, версии 1.2–1.32 — одинаково):

- `src/x-server-local.c` пишет `<run-dir>/**root**/:0` — authority самого
  X-сервера, владелец root, права 0600, пользователю недоступен. Отсюда
  и взялось имя файла `:0`;
- `src/session.c` даёт пользователю `~/.Xauthority` (умолчание) либо
  `<run-dir>/<user>/xauthority` при `user-authority-in-system-dir=true`.

Под GDM ≥ 42 файл лежит в третьем месте — `/run/user/<uid>/gdm/Xauthority`
(`gdm/daemon/gdm-x-session.c`).

Обёртка перебирает кандидатов в порядке убывания надёжности и берёт первого,
на котором сработал `xdpyinfo`:

1. `~/.Xauthority` пользователя (LightDM, `session.c`);
2. `/run/user/<uid>/gdm/Xauthority` (GDM ≥ 42);
3. `/run/lightdm/<user>/xauthority` (LightDM при
   `user-authority-in-system-dir=true`);
4. значение `-auth` из командной строки живого `Xorg` (читается
   из `/proc/<pid>/cmdline`) — тот, кто запустил сервер, знает путь точно;
5. legacy-раскладка GDM `/run/gdm3/auth-for-<user>-*/database` — последней,
   на современных сборках её нет.

Перед этим списком обёртка пробует `$XAUTHORITY`, если он задан, — но
в окружении системного юнита его нет, поэтому нумерация выше и нумерация
в замерах ниже совпадают.

Не подошёл ни один — **громкий отказ с перечислением всех проверенных путей**
и пометкой, какие из них существуют. Раньше на этом месте был немой цикл
рестарта каждые 5 секунд.

**`x11vnc -auth guess` не годится.** Его встроенный скрипт FINDDISPLAY
перебирает `~/.Xauthority`, `/tmp/.gdm*`, `/tmp/.Xauth*` и глобы
`/var/run/gdm*/auth-for-*/database` и `auth-cookie-*` — пути GDM ≥ 42 там
нет, а `auth-for-*` это legacy-раскладка. Плюс `man x11vnc` предупреждает,
что FINDDISPLAY зависает навсегда, если гритер держит X-сервер.

## Wayland

`x11vnc` обслуживает X11 и на Wayland-сессии бесполезен. Обёртка спрашивает
у `loginctl` тип сессии и отказывает внятно.

Проверять наличие сокета `/tmp/.X11-unix/X0` для этого **нельзя**: под Wayland
его создаёт mutter для Xwayland, он там есть. Собственная защита `x11vnc`
тоже не сработает — она смотрит на `$WAYLAND_DISPLAY`, которого в окружении
системного юнита нет.

Xorg включает расширение рабочего стола: `gnome` пишет `WaylandEnable=false`.
Это несущая связь между двумя расширениями, и в манифестах она не выражается.

## Подключение в VMFILE

Основной способ:

```vmfile
EXTENSION gnome
EXTENSION x11vnc X11VNC_PORT=5901
```

`EXTENSION yq` выше больше не обязателен: без него пользователь определяется
запасным путём, и тот говорит об этом в журнале. Если в образе есть
вендорская учётная запись, держите `yq` — разбор в «Порядок в VMFILE».

Ручной формы через `COPY_IN` больше нет: сборка кладёт каталог в
`/opt/bisquite/x11vnc/` и ставит рядом ссылку `lib` на общий код источника,
а это делает только `EXTENSION` (раскладка 2, см. `docs/extensions.md`).

## Проверка

На собранном образе:

```console
$ virt-cat -a образ.qcow2 /etc/bisquite/x11vnc/config
$ virt-ls -a образ.qcow2 /etc/systemd/system | grep x11vnc
```

На машине после первой загрузки:

```console
$ systemctl status configure-x11vnc.service
$ systemctl list-units 'x11vnc@*'
$ journalctl -u 'x11vnc@*' -b | tail -30     # строка «authority: …»
$ ss -ltnp | grep 5900
```

Ожидаемое при умолчаниях — `127.0.0.1:5900`; при `X11VNC_LISTEN=all` —
`0.0.0.0:5900`.

## Проверено на

| Система | Дисплей-менеджер | Что получилось | Дата |
|---|---|---|---|
| Debian 12 | GDM | authority `/run/user/1000/gdm/Xauthority` (кандидат 2), `NRestarts=0` | 2026-09-03 |
| Debian 12 | LightDM | authority `/home/check/.Xauthority` (кандидат 1), `NRestarts=0` | 2026-09-03 |
| Ubuntu 20.04, Jetson Nano (образ Q-engineering, L4T R32.6.1) | GDM3 вендора, автологин | authority `/run/user/1000/gdm/Xauthority`, сервер на `127.0.0.1`/`::1`; `bisquite-conf set x11vnc X11VNC_PASSWORD=-` со stdin — файл 0600 пользователя, перезапуск, тип безопасности VNC Authentication вместо None | 2026-09-15 |

Оба менеджера прогнаны подряд на одной машине, переключением симлинка
`display-manager.service`. Замер подтвердил и то, ради чего заведена обёртка:
прежнего пути `/var/run/lightdm/<user>/:0` нет **ни под одним** из них,
каталог `root` под LightDM существует и принадлежит X-серверу с правами 0600,
а `x11vnc -findauth` под GDM отвечает пустой строкой — то есть `-auth guess`
здесь не помог бы.
